#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

#include <list>
#include <string>

// A simple header to define several constants


// The container used to store tokens
using token_table = std::list<std::string>;

#endif
